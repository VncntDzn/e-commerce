import React from 'react';
import PropTypes from 'prop-types';

const UserPosts = (props) => {
  return (
    <div>
      {/* 
      <FluidTypography
          text={posts.productName}
          minSize='1rem'
          size='1.2vw'
          maxSize='1.9rem'
          color='black'
          fontWeight='500'
        />
        <ReactQuill
          value={posts.description}
          readOnly={true}
          theme={'bubble'}
        />

        <img className={classes.image} src={posts.links[0]} alt='nice' />

        <Box display='flex' justifyContent='flex-end' alignItems='center'>
          <FluidTypography
            text='created by: '
            minSize='0.8rem'
            size='0.9vw'
            maxSize='0.9rem'
          />
          &nbsp;
          <FluidTypography
            text={posts.author}
            minSize='0.8rem'
            size='0.9vw'
            maxSize='0.9rem'
          />
        </Box>

        <Box display='flex' justifyContent='flex-end' alignItems='center'>
          <Button
            variant='contained'
            color='secondary'
            style={{ color: 'white' }}
          >
            Review
          </Button>
          &nbsp;
          <Button
            variant='contained'
            style={{ backgroundColor: '#f44336', color: 'white' }}
          >
            Buy
          </Button>
        </Box>
      */}
    </div>
  );
};

UserPosts.propTypes = {};

export default UserPosts;
