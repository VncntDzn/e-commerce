import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '',
            main: '#FAFAFA',
            dark: ''
        },
        secondary: {
            light: '',
            main: '#FCAF18',
            dark: ''
        },
    },
});

export default theme;