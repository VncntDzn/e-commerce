import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            light: '',
            main: '#FAFAFA',
            dark: ''
        },
        secondary: {
            light: '#ffbe3b',
            main: '#FCAF18',
            dark: '#eba417'
        },
    },
});

export default theme;